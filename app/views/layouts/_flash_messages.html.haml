#flash-display
  .container
    - flash.each do |type, message|
      %div{:class => "alert alert-#{type == 'notice' ? 'info' : type} fadeout", 'data-dismiss': 'alert'}
        %button.close &times;
        = message
